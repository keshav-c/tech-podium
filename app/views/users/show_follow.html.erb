<% provide(:title, @title) %>
<div class="col-md-4 col-md-offset-4">
  <h2><%= @title %></h2>
  <% if @users.any? %>
    <ul class="users follow">
      <%= render @users %>
    </ul>
  <% end %>
</div>